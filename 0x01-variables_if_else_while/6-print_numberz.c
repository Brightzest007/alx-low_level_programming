#include <stdio.h>

/**
 *  * main - Prints numbers between 0 to 9.
 *   *
 *    * Return: Always 0 (Success)
*/
int main(void)
{
	int j;

	for (j = 0; j <= 9; j++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
